% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CohortIncidence.R
\name{createCohortIncidenceSettings}
\alias{createCohortIncidenceSettings}
\title{Create cohort incidence study settings}
\usage{
createCohortIncidenceSettings(
  studyPopulationSettings,
  outcomeIds,
  outcomeWashoutDays = 0,
  riskWindowStart = 1,
  startAnchor = "cohort start",
  riskWindowEnd = 365,
  endAnchor = "cohort start",
  byAge = FALSE,
  byGender = FALSE,
  byYear = FALSE,
  ageBreaks = NULL,
  ageBreakList = NULL,
  startDate = "",
  endDate = ""
)
}
\arguments{
\item{studyPopulationSettings}{An object created using \code{createStudyPopulationSettings} or a list of \code{createStudyPopulationSettings} that specifies cohort inclusion criteria}

\item{outcomeIds}{A vector of cohortIds for the outcome cohorts}

\item{outcomeWashoutDays}{A vector of integers specifying the washout days for the outcomeIds}

\item{riskWindowStart}{The start of the risk window (in days) relative to the `startAnchor`.}

\item{startAnchor}{The anchor point for the start of the risk window. Can be `"cohort start"`
or `"cohort end"`.}

\item{riskWindowEnd}{The end of the risk window (in days) relative to the `endAnchor`.}

\item{endAnchor}{The anchor point for the end of the risk window. Can be `"cohort start"`
or `"cohort end"`.}

\item{byAge}{Whether to stratify the incidence rates by age groups (specified via ageBreaks or ageBreakList)}

\item{byGender}{Whether to stratify the incidence rates by gender}

\item{byYear}{Whether to stratify the incidence rates by index year}

\item{ageBreaks}{a vector of integers indicating the age group bounds}

\item{ageBreakList}{a list of ageBreaks, used to specify multiple age break strata.}

\item{startDate}{a character vector representing a date in YYYY-MM-DD format}

\item{endDate}{a character vector representing a date in YYYY-MM-DD format}
}
\value{
An list with the cohort incidence settings
}
\description{
Create cohort incidence study settings
}
\examples{
# example code

ciSet <- createCohortIncidenceSettings(
  studyPopulationSettings = createStudyPopulationSettings(
    targetIds = c(1,2),
    limitToFirstInNDays = 0,
    minPriorObservation = 0
    ),
  outcomeIds = 3
)


}
\concept{CohortIncidence}
